// Empty stub for Node.js / Electron modules that must not be bundled for the browser.
// Used by vite.web.config.ts's resolve.alias to replace packages like @seald-io/nedb
// and electron with a no-op so transitive imports don't crash the web build.
export default {};
